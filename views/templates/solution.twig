{% extends 'layouts/default.twig' %}

{% block main %}
  {{ include('components/hero-double-text.twig', post.hero) }}
  {{ include('components/section-title.twig', post.section_title) }}
  {{ include('components/featured-brands.twig', post.featured_brands) }}
  {{ include('components/image-grid.twig', { rows: 1 }) }}
  {{ include('components/benefits.twig', post.benefits) }}
  {{ include('components/market-grid.twig', post.market_grid) }}
  {{ include('components/featured-articles.twig', post.featured_articles) }}
  {{ include('components/cta-section.twig', post.cta_section) }}
{% endblock %}
