<div
  @click.outside="modalOpen = false"
  class="relative z-50 mx-auto max-w-full w-3xl flex flex-col mt-(--wp-admin--admin-bar--height) px-8 pt-24 pb-12 md:py-12 md:pl-12 md:pr-14 bg-white overflow-y-auto"
>
  <button
    @click="modalOpen = false"
    @keyup.esc="modalOpen = false"
    class="group absolute top-4 right-4 md:top-2 md:right-2 size-11 p-2"
  >
    {{ include('svgs/x.twig', {
      class: '',
    }) }}
    <span class="absolute inset-0 border-4 border-blue opacity-0 transition-opacity group-hover:opacity-100"></span>
    <div class="sr-only">Close</div>
  </button>
  <div class="flex flex-col">
    <div class="flex flex-col md:flex-row gap-x-6 gap-y-7">
      {% if show_images %}
        {{ ResponsiveImage(leader.image, {
          class: 'w-full aspect-square md:w-40 object-cover',
        }) }}
      {% endif %}
      <div class="flex flex-col justify-center gap-y-1.5 lg:pr-24">
        <h3 class="text-h3">{{ leader.title }}</h3>
        <p class="text-body-large">{{ leader.job_title }}</p>
      </div>
    </div>
    <div class="h-1 w-full gradient-light bg-linear-to-l my-6 md:my-8"></div>
    <p class="text-body-medium">{{ leader.description | nl2br }}</p>
  </div>
</div>
