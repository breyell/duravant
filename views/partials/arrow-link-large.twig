<a
  class="group p-3 -m-3 inline-flex gap-x-2 items-center"
  href="{{ link }}"
  target="{{ target | default('_self') }}"
>
  <span class="text-h6">{{ title }}</span>
  {{ include('svgs/arrow-right.twig', { class: 'inline-block size-4 transition-transform group-hover:translate-x-1' }) }}
</a>
